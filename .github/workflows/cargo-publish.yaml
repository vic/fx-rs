name: cargo-publish
on:
  push:
    tags:
      - v*
jobs:
  cargo-publish:
    if: ${{ github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo -n "${{secrets.CARGO_REGISTRY_TOKEN}}" | cargo login
      - run: cargo publish 
